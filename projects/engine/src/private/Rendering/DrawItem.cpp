#include "Rendering/DrawItem.h"
#include "Rendering/GraphicsDriver.h"

namespace MAD
{
	void SDrawItem::Draw(UGraphicsDriver& inGraphicsDriver) const
	{
		(void)inGraphicsDriver;
		// Implement later
	}

}